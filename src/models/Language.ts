// -----------------------------------------------------------------------------

/** Enum specifying the supported languages. */
export enum ELanguage {
    ENGLISH = 'en',
    GERMAN  = 'de',
}

// -----------------------------------------------------------------------------